import streamlit as st
import requests
import datetime
import urllib.parse

# Настройка страницы
st.set_page_config(page_title="Тим Бёртон - Поиск новостей", layout="wide")

# Получаем Groq API ключ
if 'GROQ_API_KEY' in st.secrets:
    GROQ_API_KEY = st.secrets['GROQ_API_KEY']
else:
    st.error("Ключ GROQ_API_KEY не найден в секретах.")
    GROQ_API_KEY = None

st.title("🧛 Автоматический поиск новостей: Тим Бёртон")
st.markdown("Поиск актуальной информации, дат релизов и интервью.")

def is_burton_related(query):
    """Проверяет, связан ли запрос с Тимом Бёртоном"""
    query_lower = query.lower()
    
    # Ключевые слова, связанные с Тимом Бёртоном
    burton_keywords = [
        'тим бёртон', 'тим бертон', 'tim burton',
        'уэднесдэй', 'wednesday', 'адамс', 'addams',
        'битлджус', 'beetlejuice', 'ночной кошмар', 'nightmare before christmas',
        'джек скеллингтон', 'jack skellington', 'сонная лощина', 'sleepy hollow',
        'эдвард руки-ножницы', 'edward scissorhands', 'эдди', 'эдвард',
        'алиса в стране чудес', 'alice in wonderland', 'чарли', 'charlie',
        'шоколадная фабрика', 'chocolate factory', 'суини тодд', 'sweeney todd',
        'планета обезьян', 'planet of the apes', 'труп невесты', 'corpse bride',
        'франкенвини', 'frankenweenie', 'большие глаза', 'big eyes',
        'дамбо', 'dumbo', 'билли', 'билли бонс', 'бугимен', 'boogeyman',
        'актеры', 'фильм', 'кино', 'режиссер', 'мультфильм', 'анимация',
        'выставка', 'интервью', 'новости', 'релиз', 'премьера', 'сериал',
        'джонни депп', 'johnny depp', 'хелена бонем картер', 'helena bonham carter',
        'дэнни эльфман', 'danny elfman'
    ]
    
    # Проверяем, содержит ли запрос хотя бы одно ключевое слово
    for keyword in burton_keywords:
        if keyword in query_lower:
            return True
    
    return False

def search_news(query):
    """Поиск новостей через Groq API с запросом формата даты и ссылок"""
    if not GROQ_API_KEY:
        return None
        
    # Проверяем, связан ли запрос с Тимом Бёртоном
    if not is_burton_related(query):
        # Кодируем запрос для URL
        encoded_query = urllib.parse.quote(query)
        google_search_url = f"https://www.google.com/search?q={encoded_query}"
        
        return f"""
### ⚠️ Тематическое ограничение

Я - новостной ассистент по творчеству **Тима Бёртона**. Ваш запрос **"{query}"** не связан с фильмами, проектами или персоналиями, связанными с Тимом Бёртоном.

**Пожалуйста:**

1. **Используйте поисковые системы для новостей на другие темы:**  
   🔍 [Поиск в Google по запросу "{query}"]({google_search_url})

2. **Задайте вопрос о фильмах, актерах или проектах Тима Бёртона**

**Примеры запросов:**
- "Когда выйдет Уэднесдэй 2 сезон?"
- "Битлджус 2 дата выхода в мире"
- "Тим Бёртон и Моника Беллуччи последние новости"
- "Выставка Тима Бёртона 2024"
        """
    
    url = "https://api.groq.com/openai/v1/chat/completions"
    headers = {
        "Authorization": f"Bearer {GROQ_API_KEY}",
        "Content-Type": "application/json"
    }
    
    # Получаем текущую дату, чтобы модель понимала контекст времени
    current_date = datetime.datetime.now().strftime("%d.%m.%Y")
    
    # Обновленный промпт, требующий дату, время и ссылки
    prompt = f"""
    Сегодняшняя дата: {current_date}.
    Ты — новостной ассистент по творчеству Тима Бёртона.
    Пользователь ищет информацию по запросу: "{query}".
    
    Найди 3-4 ключевых факта или новости, связанные ТОЛЬКО с Тимом Бёртоном, его фильмами, актерами или проектами.
    Для каждой новости ОБЯЗАТЕЛЬНО используй такой формат (используй Markdown):
    
    ### 🎬 [Заголовок новости]
    📅 **Дата/Период:** [Укажи дату или примерное время события]
    🔗 **Источник:** [Укажи название источника и вставь ссылку в формате: [Название сайта](https://ссылка)]
    📝 **Суть:** [Краткое описание новости]
    
    ---
    
    Если ты не знаешь точной ссылки, укажи ссылку на официальный сайт или IMDB. Не выдумывай несуществующие URL.
    
    ВАЖНО: Отвечай ТОЛЬКО на вопросы, связанные с Тимом Бёртоном!
    """
    
    data = {
        "messages": [{"role": "user", "content": prompt}],
        "model": "llama-3.1-8b-instant", # Или llama3-70b-8192 для более точных ответов
        "temperature": 0.5 # Снижаем температуру, чтобы меньше выдумывал
    }
    
    try:
        response = requests.post(url, headers=headers, json=data)
        if response.status_code == 200:
            result = response.json()
            return result['choices'][0]['message']['content']
        else:
            return f"Ошибка API: {response.status_code}"
    except Exception as e:
        return f"Ошибка: {str(e)}"

# Интерфейс поиска
st.header("🔍 Введите тему для поиска")
search_query = st.text_input("", placeholder="Например: Битлджус 2 актеры, Выставка Бёртона 2024...")

if search_query:
    with st.spinner("Анализируем базы данных..."):
        results = search_news(search_query)
        
        if results:
            st.subheader(f"Результаты по запросу: {search_query}")
            # Используем markdown для красивого отображения ссылок и жирного текста
            st.markdown(results)
            
            # Если запрос не связан с Бёртоном, добавляем стилизованную кнопку Google
            if not is_burton_related(search_query):
                encoded_query = urllib.parse.quote(search_query)
                google_search_url = f"https://www.google.com/search?q={encoded_query}"
                
                st.markdown("---")
                st.markdown("""
                <div style='background-color: #f0f8ff; padding: 20px; border-radius: 10px; border: 2px solid #4285f4; text-align: center;'>
                    <h4 style='color: #4285f4; margin-top: 0;'>🔍 Быстрый поиск в Google</h4>
                    <p>Нажмите кнопку ниже для поиска информации по вашему запросу:</p>
                    <a href='{}' target='_blank' style='display: inline-block; background-color: #4285f4; color: white; padding: 12px 24px; text-decoration: none; border-radius: 5px; font-weight: bold; font-size: 16px;'>
                        🚀 Поиск "{}" в Google
                    </a>
                    <p style='margin-top: 10px; font-size: 12px; color: #666;'>Ссылка откроется в новой вкладке</p>
                </div>
                """.format(google_search_url, search_query), unsafe_allow_html=True)
            else:
                # Предупреждение только для Burton-запросов
                st.warning("⚠️ Примечание: Ссылки сгенерированы искусственным интеллектом. Если ссылка не открывается, попробуйте найти заголовок новости в Google.")
        else:
            st.error("Не удалось выполнить поиск. Проверьте API ключ.")

# Примеры Burton-запросов
with st.expander("📌 Примеры запросов по Тиму Бёртону (нажмите, чтобы скопировать)"):
    st.code("Когда выйдет Уэднесдэй 2 сезон?")
    st.code("Битлджус 2 дата выхода в мире")
    st.code("Тим Бёртон и Моника Беллуччи последние новости")
    st.code("Выставка Тима Бёртона 2024 в Лондоне")
    st.code("Новый фильм Тима Бёртона 2024")
    st.code("Джонни Депп и Тим Бёртон сотрудничество")

# Кнопка "Назад" с вашим дизайном
st.markdown("---")
if st.button("⬅️ Назад", use_container_width=True, key="back_news"):
    st.markdown("""
    <div style='background-color: #2b2b2b; padding: 15px; border-radius: 10px; border: 1px solid #f0e68c;'>
        <h4 style='color: #f0e68c; margin-top: 0;'>Перейти на главную страницу</h4>
        <p style='margin-bottom: 10px;'>Нажмите на ссылку ниже:</p>
        <a href='https://quixotic-shrimp-ea9.notion.site/9aabb68bd7004965819318e32d8ff06e?v=2b4a0ca7844a80d6aa8a000c6a7e5272' 
           target='_blank' 
           style='color: #ff6b6b; text-decoration: none; font-weight: bold; font-size: 16px;'>
           🏠 Главная страница проекта
        </a>
        <p style='margin-top: 10px; font-size: 12px; color: #ccc;'>Ссылка откроется в новой вкладке</p>
    </div>
    """, unsafe_allow_html=True)
