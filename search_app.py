import streamlit as st
import requests
import urllib.parse

# Настройка страницы
st.set_page_config(page_title="Тим Бёртон - Поиск новостей", layout="wide")

# Получаем Groq API ключ
API_KEY = "e9eac514f1cd4452b6f6a672b3c9cd2d"  # Ваш API ключ
GROQ_API_KEY = API_KEY if API_KEY else None

if not GROQ_API_KEY:
    st.error("Ключ API не найден.")

st.title("Автоматический поиск самых актуальных новостей о творчестве, фильмах и проектах Тима Бёртона")

def search_news(query):
    """Поиск новостей через Groq API ТОЛЬКО о Тиме Бёртоне"""
    if not GROQ_API_KEY:
        return None, False
        
    url = "https://api.groq.com/openai/v1/chat/completions"
    headers = {
        "Authorization": f"Bearer {GROQ_API_KEY}",
        "Content-Type": "application/json"
    }
    
    # Строгий промпт для фильтрации только вопросов о Тиме Бёртоне
    prompt = f"""Ты - эксперт по творчеству режиссера Тима Бёртона. Отвечай ТОЛЬКО на вопросы, связанные с Тимом Бёртоном, его фильмами, проектами, биографией и творчеством.

ИНСТРУКЦИИ:
1. Если вопрос НЕ связан с Тимом Бёртоном, отвечай: "Этот вопрос не связан с Тимом Бёртоном. Попробуйте поискать в Google."
2. Если вопрос связан с Тимом Бёртоном, найди самую актуальную информацию.
3. Отвечай на русском языке.
4. Формат для вопросов о Бёртоне: список актуальных новостей с кратким описанием.

Вопрос пользователя: {query}

Ответ:"""
    
    data = {
        "messages": [{"role": "user", "content": prompt}],
        "model": "llama-3.1-8b-instant",
        "temperature": 0.3,  # Более строгий temperature для точности
        "max_tokens": 500
    }
    
    try:
        response = requests.post(url, headers=headers, json=data)
        if response.status_code == 200:
            result = response.json()
            answer = result['choices'][0]['message']['content']
            
            # Проверяем, является ли вопрос о Тиме Бёртоне
            is_burton_related = True
            rejection_phrases = [
                "не связан с Тимом Бёртоном",
                "не могу ответить на этот вопрос",
                "этот вопрос не связан",
                "попробуйте поискать в google"
            ]
            
            for phrase in rejection_phrases:
                if phrase in answer.lower():
                    is_burton_related = False
                    break
            
            return answer, is_burton_related
        else:
            return f"Ошибка API: {response.status_code}", False
    except Exception as e:
        return f"Ошибка: {str(e)}", False

def create_google_search_link(query):
    """Создает ссылку для поиска в Google"""
    encoded_query = urllib.parse.quote(query)
    return f"https://www.google.com/search?q={encoded_query}"

# Интерфейс поиска
st.header("Поиск новостей о Тим Бёртоне")
search_query = st.text_input("Введите ваш запрос:", placeholder="Уэднесдэй 2 сезон, Битлджус 2, Джонни Депп...")

if search_query:
    with st.spinner("Ищем информацию..."):
        results, is_burton_related = search_news(search_query)
        
        if results:
            if is_burton_related:
                st.subheader("🔍 Результаты поиска по Тиму Бёртону:")
                st.write(results)
                
                # Добавляем ссылку на дополнительные поиски
                st.markdown("---")
                st.markdown(f"""
                **Хотите узнать больше?**  
                [🔎 Поискать в Google "{search_query}"]({create_google_search_link(search_query)})
                """)
            else:
                # Если вопрос не о Тиме Бёртоне
                st.warning("🤔 Этот вопрос не связан с Тимом Бёртоном")
                st.write(results)
                
                # Предлагаем поискать в Google
                st.markdown("---")
                st.subheader("🔎 Попробуйте поискать в Google:")
                google_link = create_google_search_link(search_query)
                st.markdown(f"""
                <div style='background-color: #f0f8ff; padding: 15px; border-radius: 10px; border: 1px solid #4da6ff;'>
                    <h4 style='color: #0066cc; margin-top: 0;'>Поиск в Google:</h4>
                    <a href='{google_link}' 
                       target='_blank' 
                       style='color: #ff6b6b; text-decoration: none; font-weight: bold; font-size: 16px;'>
                       🔍 Нажмите здесь для поиска: "{search_query}"
                    </a>
                    <p style='margin-top: 10px; font-size: 14px; color: #666;'>
                    Ссылка откроется в новой вкладке
                    </p>
                </div>
                """, unsafe_allow_html=True)
        else:
            st.error("Не удалось выполнить поиск")

st.header("📌 Примеры запросов о Тиме Бёртоне:")
st.markdown("""
- **Уэднесдэй 2 сезон** - новости о продолжении сериала
- **Битлджус 2** - о новом фильме Бёртона  
- **Джонни Депп и Тим Бёртон** - об их сотрудничестве
- **Новые проекты 2024** - о будущих работах
- **Тим Бёртон и Дэнни Эльфман** - о музыкальном сотрудничестве
- **Фильмы в разработке** - текущие проекты
- **Награды и премии** - достижения режиссера
- **Винсент Прайс и Бёртон** - о влиянии актера
""")

# Кнопка "Назад"
st.markdown("---")
if st.button("⬅️ Назад", use_container_width=True, key="back_news"):
    st.markdown("""
    <div style='background-color: #2b2b2b; padding: 15px; border-radius: 10px; border: 1px solid #f0e68c;'>
        <h4 style='color: #f0e68c; margin-top: 0;'>Перейти на главную страницу</h4>
        <p style='margin-bottom: 10px;'>Нажмите на ссылку ниже:</p>
        <a href='https://quixotic-shrimp-ea9.notion.site/9aabb68bd7004965819318e32d8ff06e?v=2b4a0ca7844a80d6aa8a000c6a7e5272' 
           target='_blank' 
           style='color: #ff6b6b; text-decoration: none; font-weight: bold; font-size: 16px;'>
           🏠 Главная страница проекта
        </a>
        <p style='margin-top: 10px; font-size: 12px; color: #ccc;'>Ссылка откроется в новой вкладке</p>
    </div>
    """, unsafe_allow_html=True)

# Информация о системе
with st.sidebar:
    st.markdown("### ℹ️ О системе поиска")
    st.markdown("""
    Эта система ищет информацию **только о Тиме Бёртоне**:
    
    ✅ **Поиск по:**
    - Фильмам и проектам
    - Новостям и анонсам
    - Сотрудничеству с актерами
    - Биографии и творчество
    
    Для других вопросов будет предложен поиск в Google.
    """)
